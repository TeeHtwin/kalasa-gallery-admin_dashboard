type LoginData = {
  email: string;
  password: string;
  callbackUrl: string;
};

type Collection = {
  id: number;
  title: string;
  description: string;
  image: string;
};

type Contact = {
  id: number;
};
