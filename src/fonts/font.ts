import { Cardo, Arima } from 'next/font/google';

export const cardo = Cardo({
  weight: ['400', '700'],
  subsets: ['latin'],
  variable: '--font-cardo',
});

export const arial = Arima({
  weight: ['400', '500'],
  subsets: ['latin'],
  variable: '--font-ariel',
});
